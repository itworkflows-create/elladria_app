# This function verifies the bearer JWT with Auth.getUser before any deletion.
# Keep this explicit verification when using publishable keys/new JWT signing keys.
[functions.delete-account]
verify_jwt = false

[functions.send-notification]
verify_jwt = false
